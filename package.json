{
  "name": "twilio-sip-router",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "devDependencies": {
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "12"
  },
  "dependencies": {
    "google-libphonenumber": "^3.2.21"
  }
}
